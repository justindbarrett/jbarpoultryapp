import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.jbarpoultry.app',
  appName: 'J-Bar Poultry',
  webDir: './www/browser'
};

export default config;
